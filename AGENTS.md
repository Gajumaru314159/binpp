## タスク完了後の注意事項

本拡張機能の開発タスクが完了した後は、必ずTypeScriptのコンパイルを実行してください。

コンパイルは以下のコマンドで行います。

```
npm run compile
```

---

## 無視ファイルに関する注意

以下のディレクトリはビルドディレクトリのため読み込み不要です。

- node_modules/
- out/

## 重要

ユーザーはClineよりプログラミングが得意ですが、時短のためにClineにコーディングを依頼しています。

ただし、ClineはGitHubから学習した広範な知識を持っており、個別のアルゴリズムやライブラリの使い方は私が実装するよりも速いでしょう。

反面、現在のコンテキストに応じた処理は苦手です。コンテキストが不明瞭な時は、ユーザーに確認します。

ファイルは適宜分割して巨大なソースファイルにならないように注意してください。

作業状況がわかるようにAPIをリクエストする場合はリクエスト内容を説明してください。

## 仕様書

使用はdoc/specification.mdを参照してください。

